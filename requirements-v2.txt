# Upper bounds pin FastAPI/Starlette to the tested-good line: the v2 404/405
# route-introspection details (route_not_found_details) depend on Starlette's
# route API, which shifted in newer releases and broke test_v2_app_launcher in CI.
# Verified good locally at fastapi 0.128.8 / starlette 0.49.3.
fastapi>=0.116,<0.129
starlette>=0.47,<0.50
httpx>=0.27
uvicorn>=0.35
rich>=13.7
prompt_toolkit>=3.0
# requests is a runtime dependency of the proxy (do-anthropic-proxy.py); it was
# previously undeclared, so CI and RPM builds lacked it.
requests>=2.31,<3
